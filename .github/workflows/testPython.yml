name : Python CI

on :
workflow_dispatch :
push: 
  branches:
    - main

jobs: 
  tests :
    runs-on : ubuntu-latest

    steps :
      - uses: actions/checkout@v3

      
      - name: print folder
      - run:  |
          ls
          mkdir folder
